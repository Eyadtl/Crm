APP_NAME="Arabia Talents CRM"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=arabia_talents_crm
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_SSLMODE=prefer

SUPABASE_STORAGE_BUCKET=email-bodies
SUPABASE_STORAGE_KEY=local-storage-key
SUPABASE_STORAGE_URL=http://localhost:54321/storage/v1
SUPABASE_SIGNING_KEY=dev-signing-key

CRON_SHARED_SECRET=change-me
EMAIL_HOT_STORAGE_MONTHS=6
IMAP_BODY_TTL_DAYS=7
IMAP_MAX_ATTACHMENT_MB=20
JOB_MAX_RETRIES=3
IMAP_DEFAULT_FOLDER=INBOX
IMAP_VALIDATE_CERT=true
IMAP_TIMEOUT=60
IMAP_MAX_PER_SYNC=200
SMTP_TIMEOUT=60
SMTP_VALIDATE_CERT=true
EMAIL_ATTACHMENT_DISK=local
EMAIL_BODY_DISK=local
MAILBOX_SKIP_CONNECTIVITY=false
MAILBOX_FAKE_SYNC=false

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=supabase
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_SCHEME=tls
MAIL_HOST=127.0.0.1
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

LOGIN_RATE_LIMIT_PER_MINUTE=10

VITE_APP_NAME="${APP_NAME}"
VITE_API_URL=${APP_URL}/api/v1
